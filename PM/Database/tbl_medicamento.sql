CREATE TABLE [dbo].[tbl_medicamento]
(
	[Nro_registro] INT NOT NULL PRIMARY KEY, 
    [Nome] VARCHAR(100) NOT NULL, 
    [Principio_ativo ] VARCHAR(100) NOT NULL, 
    [Posologia ] VARCHAR(20) NOT NULL
)
